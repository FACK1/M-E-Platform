const activity = require('./activity.controller');
const program = require('./program.controller');
const user = require('./user.controller');
const objective = require('./objective.controller');
const auth = require('./auth.controller');

module.exports = {
  activity,
  program,
  user,
  objective,
  auth,
};
